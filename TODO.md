# TODO

## 1 — Default Permissions / Auto-Approved Commands

**Goal:** Provide a curated set of default permissions — safe, read-only commands (e.g. `git diff`, `diff`, `git status`, `git log`, `ls`) auto-approved without prompting — that deploy to all supported harnesses (`claude`, `copilot`).

**Priority: medium (concrete user value).**

### Background

Each harness expresses its command allow-list differently, and it's unconfirmed whether APM can deploy permission/settings blocks per target (it currently handles skills, agents, prompts, instructions, hooks, and MCP).

| Tool            | File            | Setting (to verify)                                           |
| --------------- | --------------- | ------------------------------------------------------------- |
| Claude Code     | `settings.json` | `permissions.allow` — e.g. `Bash(git diff:*)`, `Bash(diff:*)` |
| VS Code Copilot | `settings.json` | terminal auto-approve allow-list (confirm exact key)          |
| APM             | `apm.yml`       | confirm whether permission/settings deployment is supported   |

Related: the `fewer-permission-prompts` skill derives allow-lists from transcripts — useful as a source for the default set.

### Guardrail hooks — the portable deny-side complement

An allow-list is per-harness settings config (possibly undeployable via APM); a deterministic **deny** guard is a hook, which APM already deploys to every target. [dirien/my-claude-apm-setup](https://github.com/dirien/my-claude-apm-setup) wires two POSIX scripts through a single [`.apm/hooks/guardrails.json`](https://github.com/dirien/my-claude-apm-setup/blob/main/.apm/hooks/guardrails.json):

| Hook | Script | Behaviour |
| ------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------ |
| `PreToolUse`  | [`scripts/guard.sh`](https://github.com/dirien/my-claude-apm-setup/blob/main/scripts/guard.sh)     | Blocks destructive Bash — `rm -rf /`, `git push --force`, `git reset --hard`, `mkfs`, `dd if=` — by exiting 2        |
| `PostToolUse` | [`scripts/post-edit.sh`](https://github.com/dirien/my-claude-apm-setup/blob/main/scripts/post-edit.sh) | Blocks writes adding a credential (AWS key, private key, `ghp_…`, `sk-…`, Slack token); formats with `gofmt`/`prettier`/`ruff` when present |

Both read the hook JSON on stdin and exit 2 to block. Exit 2 holds up for the `PreToolUse` guard, but not for `PostToolUse`: it is ambiguous there on Claude and ignored by the Copilot CLI. Port `post-edit.sh` to the per-harness JSON output in the [meta-harness hook contract](packages/baseline/.apm/skills/meta-harness/references/hooks.md#input-and-output-contract). Note the repo convention prefers cross-platform Python/Node hook scripts over POSIX shell ([meta-harness §6](packages/baseline/.apm/skills/meta-harness/SKILL.md#6-this-repositorys-conventions)), so port rather than copy.

### Tasks

- [ ] **1a — Investigate APM support** for deploying permission/settings configuration across targets; if unsupported, track upstream (file/find an issue) or deploy out-of-band.
- [ ] **1b — Define the default allow-list** of safe read-only commands.
- [ ] **1c — Map the allow-list to each target's native format** and verify the deployed result with `apm install -g`.
- [ ] **1d — Add a deny-side guardrail hook pair** (destructive-command guard + secret-write guard) as cross-platform scripts, following the pattern above and the [meta-harness skill](packages/baseline/.apm/skills/meta-harness/SKILL.md). Independent of 1a — hook deployment is already supported.

---

## 2 — LSP Server Configuration Support

**Goal:** Document and provide cross-tool compatible LSP (Language Server Protocol) server configuration guidance within `.llmctl`, analogous to the MCP support — so agent harnesses can be configured with language servers (diagnostics, hover, go-to-definition, etc.) as a deployable unit via APM.

**Priority: medium-low (investigative, mirrors the MCP pattern).**

### Background

Agent harnesses increasingly support configuring LSP servers to give agents richer code intelligence. As with MCP, the config format likely differs across tools in file location and structure. **APM support is confirmed** (see below); the per-harness rows still need verification against current docs before authoring.

| Tool             | File          | Format        | Key/Section                                                                 |
| ---------------- | ------------- | ------------- | ----------------------------------------------------------------------------- |
| VS Code Copilot  | _(to verify)_ | _(to verify)_ | _(to verify)_                                                                 |
| Claude Code      | _(to verify)_ | _(to verify)_ | _(to verify)_                                                                 |
| OpenAI Codex CLI | _(to verify)_ | _(to verify)_ | _(to verify)_                                                                 |
| APM              | `apm.yml`     | YAML          | `dependencies.lsp` — **supported**, emits `.lsp.json` (verified on APM 0.22)  |

**APM block shape**, from [dirien/my-claude-apm-setup/apm.yml](https://github.com/dirien/my-claude-apm-setup/blob/main/apm.yml) (four servers: `gopls`, `typescript-language-server`, `pyright`, `csharp-ls`):

```yaml
dependencies:
  lsp:
    - name: gopls
      command: gopls
      args: ["serve"]
      extensionToLanguage:
        ".go": go
```

APM writes the config only — the binaries are the user's responsibility, exactly like the MCP pattern. Open questions: whether `.lsp.json` is a single harness-neutral file or gets per-target placement on 0.26, and which harnesses actually read it.

**Companion content — adapt, do not consume.** Config alone just adds an unused capability; the *usage* half is [antonbabenko/agent-plugins → `code-intelligence`](https://github.com/antonbabenko/agent-plugins/tree/master/plugins/code-intelligence) (Apache-2.0): tool precedence (LSP for symbol relationships, `rg` for exact text, semantic search for "where might this live"), position-anchored LSP calls, a three-part degradation gate before declaring the server unavailable, first-line disclosure of any tool substitution, and a proof requirement before claiming a tool is missing.

**Verdict: not properly cross-harness — copy but adapt to more generic tool naming.** Its *vocabulary* is already neutral ("host exposes no language-server tool", "semantic/neural search, if host provides", "host-approved text search") — it names no Claude tool. Its *procedure*, however, is LSP-protocol-shaped: `documentSymbol` probes, cold-start retries, `prepareRename`, call hierarchy. On an IDE-integrated harness (VS Code Copilot) the equivalent intelligence arrives as IDE diagnostics and editor navigation rather than position-anchored protocol calls, so its degradation gate would classify a fully-capable IDE session as "no LSP at all → fall back to text search" and under-use what is actually there. The adaptation is a three-tier capability model — **LSP tool | IDE diagnostics/navigation | text search** — with the precedence, disclosure, and anti-phantom-shim rules kept as-is. Its `/setup-code-intelligence` command is POSIX-only (`uname -s`, `command -v`) and would need a cross-platform rewrite if wanted.

See also [terraform-skill's `references/code-intelligence-lsp.md`](https://github.com/antonbabenko/terraform-skill/blob/master/skills/terraform-skill/references/code-intelligence-lsp.md) for a domain-specific extension (the `terraform-ls` capability matrix) — that dependency is already installed via `packages/ops`.

### Tasks

- [ ] **2a — Investigate per-tool LSP config support and format** across `claude` and `copilot` (and Codex if relevant); fill in the matrix above from authoritative docs. Lead from the 2026-08-01 upstream survey: [github/awesome-copilot `skills/lsp-setup`](https://github.com/github/awesome-copilot/tree/main/skills/lsp-setup) (MIT, subdir-consumable) — check whether it already documents the per-harness matrix.
- [ ] **2b — Verify the `dependencies.lsp` block against the installed APM version** (0.26) — confirm output path(s), per-target behaviour, and whether transitive LSP deps survive like/unlike MCP; the shape above is verified only on 0.22.
- [ ] **2c — Create an LSP configuration reference** as `packages/baseline/.apm/skills/meta-harness/references/lsp.md`, matching the existing `mcp.md` / `mcp-configuration.md` pattern, documenting the per-tool config matrix and the APM block. `.lsp.json` is harness-executed config, so it belongs behind the `meta-harness` router rather than as a separate skill — add a row to that router's routing table.
- [ ] **2d — Create a cross-tool LSP setup prompt** (e.g. `packages/baseline/.apm/prompts/setup-lsp.prompt.md`, beside `setup-mcp`), scoped to emit the APM dependency block and delegating schema knowledge to `meta-harness`.
- [ ] **2e — Update `README.md`** — add an LSP Servers row to the steering matrix and a subsection referencing the `meta-lsp` skill.
- [ ] **2f — Adapt `code-intelligence` into a local skill** (`packages/baseline`, or `packages/workflow` if it should load only for code work) once 2a–2c land: copy the precedence/anchoring/degradation/disclosure rules, generalise to the three-tier capability model above, and record `metadata.provenance.adaptedFrom: https://github.com/antonbabenko/agent-plugins/blob/master/plugins/code-intelligence/skills/code-intelligence/SKILL.md`. Gated on 2a — the tiers can only be named once the per-harness matrix is filled in.

---

## 3 — Upstream APM Tracking / Add When Needed

**Priority: waiting.** Ongoing tracking and items gated on a demonstrated need.

- [ ] **3a — Track upstream APM evolution (esp. deployed-file gitignore / local-settings targeting).** APM deploys Claude hook wiring only to `settings.json` (project) or `~/.claude/settings.json` (user) — it cannot target the git-ignored `settings.local.json` variant ([hook_integrator.py](https://github.com/microsoft/apm/blob/main/src/apm_cli/integration/hook_integrator.py) hardcodes `config_filename="settings.json"`). Watch microsoft/apm [#1342](https://github.com/microsoft/apm/issues/1342) (related: [#990](https://github.com/microsoft/apm/issues/990), [#290](https://github.com/microsoft/apm/issues/290)). `.claude/`, `.codex/` and `.agents/` are therefore git-ignored in full, which costs a fresh clone its hook wiring until `apm install` runs and leaves nowhere to commit shared project settings. When a `.local`-target option or deployed-file gitignore mode ships, revisit the [`.gitignore`](.gitignore) comment above those entries and the deploy guidance in [CONTRIBUTING.md](CONTRIBUTING.md#hooks-hookjson). More broadly, periodically review APM releases for changes affecting this repo's deploy assumptions.
- [ ] **3b — First plugin:** Add a plugin bundle when tool/MCP capabilities are insufficient for a task. Authoring guidance lives in the [meta-harness skill](packages/baseline/.apm/skills/meta-harness/SKILL.md), which routes to `references/plugins.md`.
- [ ] **3c — Track two APM 0.25 environment issues found during clean redeploy.**

  1. **Aggregator not `-g`-installable (aggregator removed, reinstate later).** A prototyped `packages/global` aggregator (an `apm.yml` with only `dependencies.apm` → `../core`, `../meta`) resolved its transitive *local-path* deps but deployed **zero** primitives at user scope (worked at project scope). It was **removed** to avoid documenting a broken command; global install names `core` + `workflow` directly for now (`meta` has since been absorbed into `core`). **To reinstate:** recreate `packages/global/apm.yml` (deps: `./../core`, `./../workflow`, + third-party global recs), point `~/.apm/apm.yml` at it, and make it the single-command global profile + home for third-party global recommendations. File/find an upstream APM issue for transitive local-path deploy at `-g`; do this once it's fixed.
  2. **`copilot-cowork` + multiple OneDrive mounts** aborts any global install at lockfile generation (`--exclude copilot-cowork` does not help). Workaround: export `APM_COPILOT_COWORK_SKILLS_DIR` to a single dir. Noted in README prerequisites.
- [ ] **3d — Track APM per-target agent tool-policy mapping.** APM 0.26.0 copies agent frontmatter verbatim to every target (no per-target normalization, unlike hooks), so a Copilot `tools:` array deploys unchanged to `~/.claude/agents/` and makes the agent unspawnable on Claude Code. Repo agents work around this by omitting `tools:` and scoping Claude via `disallowedTools` (see the [meta-steering router](packages/baseline/.apm/skills/meta-steering/SKILL.md#4-frontmatter-shared-by-all-four-types)); the trade-off is that Copilot loses allow-list scoping (inherits all tools). Watch microsoft/apm [#2108](https://github.com/microsoft/apm/issues/2108) (portable agent semantics) under epic [#2112](https://github.com/microsoft/apm/issues/2112); related: [#293](https://github.com/microsoft/apm/issues/293) (consumer tool overrides), [#2261](https://github.com/microsoft/apm/issues/2261) (per-file target declaration — would let us stop deploying `plan`/`explore` to Claude), [#581](https://github.com/microsoft/apm/issues/581) (declined intra-file translation precedent). When per-target agent frontmatter mapping ships, restore precise Copilot allow-lists.
- [ ] **3e — Get agent model selection solved upstream, not in this repo.** An agent file carries one `model:` value, and APM copies it verbatim to Claude and Copilot and strips it for Codex. So `metadata.modelProfile` can only resolve to a Claude alias (`haiku`/`sonnet`/`opus` + `effort`). Copilot's qualified names are left as non-functional comments, and a Codex agent inherits the session model. The [`meta-update-models`](.apm/skills/meta-update-models/SKILL.md) maps are a Claude-first workaround, and a portable profile would not fix that here: there is no cross-harness model vocabulary for it to resolve to, and APM has nowhere per target to write the result. The real fix belongs to APM: a portable model intent (tier, reasoning effort, cost) that the deployer resolves per target — Claude alias, Copilot model list, Codex `model`/`model_reasoning_effort` in the agent TOML — or at minimum per-target overrides of `model`. The closest tracker is microsoft/apm [#2108](https://github.com/microsoft/apm/issues/2108) (portable agent semantics) under epic [#2112](https://github.com/microsoft/apm/issues/2112); neither covers model selection, and a search on 2026-09-24 found no request that does. **Next step:** file one that references #2108 and uses `modelProfile` as the worked example. When it ships, retire the Claude-only resolution in `meta-update-models` and re-derive each agent's profile against it. Until then, do not grow the local resolver toward per-harness output.
- [ ] **3f — Track APM plugin-pack hook handling before any package ships a hook.** APM 0.31's `apm pack --format plugin` breaks hooks in three ways. It moves `.apm/skills/` to `skills/` but leaves hook commands pointing at `${CLAUDE_PLUGIN_ROOT}/.apm/skills/…`. It does not copy scripts referenced from `.apm/hooks/`. And it writes `hooks.json` at the bundle root without a `hooks` key in `plugin.json`, so Claude Code (which looks for `hooks/hooks.json` by default) never loads them. Filed as microsoft/apm [#3074](https://github.com/microsoft/apm/issues/3074) on 2026-09-25. The downstream normalizer in PR [#33](https://github.com/siegenthalerroger/.llmctl/pull/33) was parked in favour of the upstream fix. **One packaged hook now exists and is knowingly inert in bundles:** `baseline`'s [always-on-instructions](packages/baseline/.apm/hooks/always-on-instructions.hook.json) SessionStart hook, which feeds the instruction files named in its command to the session. Under `apm install` it is silent in Claude Code, where the files deploy as rules, and fills the gaps for Codex (no `AGENTS.md` without `apm compile`) and Copilot CLI (no auto-apply without `applyTo`). It was verified in Claude Code against a hand-fixed bundle; in a packed bundle its script is missing and `hooks.json` is undiscovered. Until #3074 ships, [ALWAYS_ON_INSTRUCTIONS.md](README.marketplace.md#always-on-instructions) in the marketplace is the only route for those instructions. Not yet verified in a live session: Codex (plugin and `apm install` hooks both need trusting in `/hooks` first) and Copilot CLI; both were checked only by running the deployed script with each harness's documented payload. Dropping the `.apm/` nesting is not a way around it: it would fix the path problem but not the discovery one, and on 0.31 it stops `apm install` deploying agents, instructions, prompts and hooks, and `apm pack` drops prompts (microsoft/apm [#1161](https://github.com/microsoft/apm/issues/1161); see the [packaging model](CONTRIBUTING.md#packaging-model)). **Before 1d or any further packaged hook lands:** check whether #3074 has shipped. If not, add a manifest `"hooks": "./hooks.json"` and a pack-time check that fails when a hook command references a path missing from the bundle.
- [ ] **3g — Retire the packer's manifest post-processing once APM generates it.** [`pack_marketplace.py`](src/llmctl/pack_marketplace.py) patches every bundle after `apm pack` in two ways. `apply_display_name` copies each package's `displayName:` into the Claude manifest, because APM 0.31 accepts the key in `apm.yml` and then drops it. And `write_codex_manifest` builds `.codex-plugin/plugin.json` from the Claude manifest, because APM generates only the Claude and Copilot manifests, and Codex won't install a marketplace entry without its own. Filed as microsoft/apm [#3076](https://github.com/microsoft/apm/issues/3076) (top-level `displayName`) and [#3077](https://github.com/microsoft/apm/issues/3077) (Codex manifest) on 2026-09-25. The #1621 fields (object `author`, `homepage`, `repository`, `keywords`) already reach the Claude manifest from `apm.yml`, so the packer only copies them into the Codex one. **When either ships:** delete the matching step, repack, and diff the bundles against the last release to confirm nothing a host reads has changed. For Codex, check `interface.category` in particular: it comes from the package's `category:`, which APM does not read.
- [ ] **3h — Unblock `apm install PLUGIN@llmctl-marketplace`.** On APM 0.31 no plugin installs from the git-hosted marketplace. `apm pack` names each bundle `plugins/<name>-<version>/`, the catalogue `source` points there, and install validation reads the dotted version as a file extension ("Invalid virtual package path … should not have a file extension"). Claude Code, Codex and VS Code install the same entries fine, and a marketplace registered from a local path works too. Filed as microsoft/apm [#3078](https://github.com/microsoft/apm/issues/3078) on 2026-09-25; related [#2432](https://github.com/microsoft/apm/issues/2432) asks pack to drop the `<name>-<version>/` level. Until then, APM-managed projects stay on direct `packages/<dir>` dependencies. **Fallback, if #3078 stalls:** have [`pack_marketplace.py`](src/llmctl/pack_marketplace.py) move each bundle to `plugins/<name>/` and emit that as its `source`; the version already lives in `plugin.json` and the catalogue, so only the packer's keep-or-repack check, which matches on the folder name, has to read it from `plugin.json` instead. **When it ships:** switch the project manifests to `PLUGIN@llmctl-marketplace` and re-run the repro in the issue.

---

## 4 — CI for Steering-File Correctness and Deployability

**Goal:** Catch structurally broken or undeployable steering content before it lands, instead of relying on local edit-time hooks and manual `apm install -g` runs.

**Priority: medium-high (protects every other workstream).**

### Background

Two validation layers exist as of 2026-08-02, and neither covers this workstream:

- [check_frontmatter.py](src/llmctl/check_frontmatter.py), wired as a Claude Code `PostToolUse` hook — in that mode it fires only on files edited in a Claude session, so drift in untouched files, edits made from other harnesses, and APM version skew all go unnoticed until a deploy misbehaves (cf. **3c**, **3d**).
- [check.py](src/llmctl/check.py) — frontmatter, the commit convention, licences, lockfiles against their pins, and a full scratch pack. Repo-wide and run in CI on every pull request, but it covers provenance, licensing and the release path rather than the semantic conventions below.
Two distinct failure classes are worth checking separately:

| Class | Question | Signal |
| --- | --- | --- |
| Semantic correctness | Is each file well-formed per the repo's conventions? | Frontmatter fields, name↔directory match, kebab-case, description length/single-line, reserved words, provenance metadata |
| Deployability | Does APM actually see and place every item? | Every authored primitive appears at the expected path for each target after an install into a throwaway home/workspace |

**APM ships most of the deployability gate already.** Commands verified against APM 0.22 in [dirien/my-claude-apm-setup → docs/APM-ADOPTION.md](https://github.com/dirien/my-claude-apm-setup/blob/main/docs/APM-ADOPTION.md); re-verify on 0.26 before wiring:

| Command | What it gives us |
| ---------------------------------------------------- | -------------------------------------------------------------------------------------- |
| `apm compile -t claude,copilot --validate`            | Validates every primitive, writes nothing — cheapest gate, covers part of **4a**        |
| `apm install --frozen`                                | Reproducible install from `apm.lock.yaml` (the `npm ci` equivalent) — basis for **4d**   |
| `apm audit --ci`                                      | Fails on orphaned / undeployed packages                                                  |
| `apm compile --clean --local-only` + `git diff --exit-code` | Drift check for generated files                                                    |
| `apm lock export -f cyclonedx\|spdx --timestamp`      | Offline, reproducible SBOM of the pinned APM dependencies in `packages/*/apm.yml`     |
| `apm outdated` → `apm update --dry-run` → `apm update -y` | Maintenance loop for the SHA-pinned deps in `packages/*/apm.yml`                                     |

Two caveats from that repo: `devDependencies` entries are reported as orphaned by `apm audit --ci` under a frozen install, and generated files carry an APM-version stamp that trips a hard `git diff` gate whenever the runner's APM is newer than the author's — which is why they keep the drift check local rather than in CI.

### Tasks

- [x] **4a — Run the frontmatter validator over the whole tree in CI.** **Done 2026-08-19.** [check_frontmatter.py](src/llmctl/check_frontmatter.py) validates the whole tree alongside its stdin `--hook` mode — one implementation, two entry points, and the batch mode exits 1 on errors rather than the hook contract's 2. It runs as check.py's cheapest gate, so it fires on every PR, every push, and any local `apm run check`.
- [ ] **4b — Extend validation to metadata this repo relies on but the hook ignores** — `model:` frontmatter on agents and the `tools:`-omission convention from **3d**.

  **The provenance half of this task shipped.** [check_licenses.py](src/llmctl/check_licenses.py) now hard-errors on an `adaptedFrom` block that parses to zero URLs and on an object entry with no `url`, and it parses through the shared [provenance.py](src/llmctl/provenance.py) so the validator and the [drift audit](src/llmctl/check_updates.py) cannot disagree about what counts as tracked — they run the same parse, since the audit was rewritten from PowerShell into Python. It also requires an upstream `license` wherever `fidelity` copies expression. Two lint-grade checks from the original list remain unimplemented, both warnings rather than errors:

  - a `took` value written as a block scalar (`|` / `>`) — no longer a parsing hazard: since the audit moved to Python both consumers read it through python-frontmatter and keep the content, newlines included. What remains is style (the text report prints one line per row), so this is a warning at most
  - a `took` value containing `Not taken` / `Original locally`, or a `%` measurement — removed from the convention on 2026-08-01 because they rot with no local change to trigger a refresh; see [meta-steering's provenance section](packages/baseline/.apm/skills/meta-steering/references/skill-frontmatter.md#provenance-metadata-recommended)

- [x] **4c — Fix the drift audit's dead-URL blind spot.** **Done 2026-09-17.** [check_updates.py](src/llmctl/check_updates.py) now probes `contents/<path>` before comparing dates and reports `source_missing` with recommendation `repoint_or_drop_provenance`. Original finding: Found 2026-08-01 while annotating `took`: [check_updates.py](src/llmctl/check_updates.py) never verifies the provenance path still **exists**. `GET /repos/{o}/{r}/commits?path=<deleted-path>` happily returns the commit that *deleted* the path, so the script reads a real date, compares it, and reports `up_to_date` — forever. Two of the repo's own entries were pointing at 404s and reporting healthy: `researcher-advanced` (upstream restructured 2026-05-29) and `code-reviewer` (moved into `skills/requesting-code-review/`). Both re-pointed; the blind spot remains. Fix: probe `contents/<path>` (or check whether the newest commit for the path is a deletion) and emit a distinct `source_missing` status with recommendation `repoint_or_drop_provenance`. Without this, provenance rots silently, which defeats the point of tracking it.
- [ ] **4d — Add a deployability check.** Install each package into a disposable `HOME`/workspace and assert that every authored primitive lands in the expected per-target location, comparing against an inventory derived from `packages/*/.apm/`. Confirm the cheapest reliable mechanism first (lockfile inspection, a dry-run flag, or a real install into a temp dir) rather than assuming one exists.
- [x] **4j — Decide whether `scripts/` stays a set of single files.** **Done 2026-09-19: it is a project.** [pyproject.toml](pyproject.toml), `src/llmctl/`, one `uv.lock`, and one `llmctl-<command>` console script per command — still separate commands rather than one CLI with subcommands, because `apm run` passes nothing through, so a single entry point would have collapsed nothing in `apm.yml`. The nine identical PEP 723 headers and the gate that kept them identical are gone; the `tooling` gate holds `uv.lock` to `pyproject.toml` instead. This is also what freed the private workspace from the sibling clone: its `apm.yml` and workflows run the commands with `uvx --from git+…@main`, and name this repository's composite actions directly. Still open from the note below: a type checker and tests, which now have a project to live in. Original note: Its own `python-scripts` skill (§9) says it has already graduated: 14 files and ~3,600 lines, 8 entry scripts importing 6 shared modules, and a `scripts` gate whose whole job is keeping 8 duplicated PEP 723 headers byte-identical. `python-standards` §1 puts that at the shared-code tier, where a type checker and tests stop being polish. **Kept single-file deliberately on 2026-09-18**, because that form is what makes `uv run scripts/check.py` work with nothing installed first, here and in the private workspace. Every signature was annotated the same day, so what remains is a checker and tests — neither of which needs a `pyproject.toml` to start. Packaging it (`src/`, console entry points, one lockfile) is also what would let the private workspace run the tooling from a pinned git ref rather than a sibling clone, so that decision and this one move together.

- [ ] **4l — `apm install --frozen` cannot restore a package that declares MCP servers.** Found 2026-09-18, when `packages/core` (now `packages/baseline`) became a root dependency and CI's deploy step went red on a clean checkout: `apm install --frozen` aborts with "package manifest not found at `apm_modules/blader/humanizer/apm.yml`; re-run `apm install` to restore it" before it fetches anything. On APM 0.31.0 it appears only while `dependencies.mcp` is non-empty — the same three dependencies with `mcp: []` restore frozen without complaint, and a warm `apm_modules/` hides it entirely. Two conditions are present together and only one was toggled: the error names the manifestless `blader/humanizer`, so whether an MCP block alone (without a manifestless dependency) triggers it is **not established**, and neither is whether `ops`, which declares MCP servers too, is affected — only `core` was observed. It is the reason the workflows' deploy step installs unfrozen. Re-test it on every APM CLI bump (the `meta-update-repo` skill's APM CLI phase), including an `ops` cold-cache restore. Related to the R1 note behind [Lockfiles](CONTRIBUTING.md#lockfiles): `--frozen` was already the wrong tool for holding a lockfile to its pins, which is the `lockfiles` gate's job. Report it upstream, and drop the workaround when a release fixes it.
- [ ] **4m — Flag prompt and agent body links that break once deployed.** Found 2026-09-24 by the `meta-review-steering` sweep: `setup-mcp.prompt.md` linked `[apm.yml](../../../../apm.yml)`, which resolves only inside this repository's layout. APM rewrites a relative body link that resolves into a package (a skill link becomes a path under the package's `.apm/` or `apm_modules/`) and copies every other one unchanged, so it breaks at the deployed location ([deployer caveats](packages/baseline/.apm/skills/meta-steering/references/frontmatter-deploy.md#deployer-caveats)). The `frontmatter` gate did not catch it. Add a check to [check_frontmatter.py](src/llmctl/check_frontmatter.py): for every relative link in a `*.prompt.md` or `*.agent.md` body, fail when the target does not exist or resolves outside `packages/*/.apm/`, and tell the author to link repository files by URL or by name. Offline and cheap. It is the deploy-aware half of **4e**, which checks that links resolve at all.
- [ ] **4n — Reject `skills:` in prompt frontmatter.** The refreshed matrix classifies it as *Not a key*: no harness reads `skills:` on a prompt, and APM drops it for Claude commands ([prompts.md](packages/baseline/.apm/skills/meta-steering/references/prompts.md#frontmatter-fields)). Binding a skill from a prompt has no frontmatter form, so the body link is the only binding. `plan-trip.prompt.md` and `setup-mcp.prompt.md` still carry it, and both already link their skill in the body. Add a `frontmatter` rule that errors on `skills:` in a `*.prompt.md` and says to link the skill in the body instead, then remove the key from those two files in the same change. It is deterministic, so it belongs in the gate rather than in review prose.
- [ ] **4k — Find a source for the upstream safety-review rows, or say there is none.** [references/safety-review.md](.apm/skills/meta-update-repo/references/safety-review.md) lists what to read an upstream diff for — assistant-directed text, credential access, new executable surface, obfuscated payloads, scope creep. The rows were reasoned from what an APM dependency can do once deployed, not taken from anything, and the file cites nothing. Two outcomes are both fine, but neither is the current silence: find published guidance on reviewing agent-facing dependencies (a supply-chain or prompt-injection threat model, a harness vendor's own plugin-review advice) and record it under `authoritativeSpec`, reconciling any row that disagrees with it; or establish there is none yet and say so in the file, so the next reader knows the rows are this repository's own judgement rather than an uncited standard.
- [ ] **4e — Add link/reference integrity checking** for relative markdown links (the README/TODO/CONTRIBUTING cross-links and skill→reference paths); optionally check external links on a schedule only, to avoid flaky PR runs.
- [x] **4f — Wire it into GitHub Actions.** **Done 2026-09-17.** There is one gate set now: the marketplace-shaped gates pack into a scratch directory instead of needing the other repository checked out, so a pull request runs all of them. `checks.yml` covers pull requests; `release.yml` runs the same gates on a push to `main` and then releases. The marketplace repositories have no workflow left. Original note:: run 4a–4e on PRs, plus a scheduled run so a new APM release that breaks deployment surfaces on its own rather than during the next manual deploy (ties into **3a**).

  **Partly done 2026-08-19.** The `checks.yml` written on 2026-08-02 had never run once: `.gitignore` ignored all of `.github/*`, because APM deploys Copilot primitives there, so the file lived only in the author's working tree. It is committed now, with the ignore re-including `.github/workflows/` alone.

  - `checks.yml` — every release gate on PRs and pushes to `main`.
  - `release.yml` — the gates, then `release.py` with the inputs chosen at dispatch; a dry run by default.

  Superseded since: the marketplace repos' own `checks.yml` is gone (nothing there is authored), and the shared steps of all four workflows now live in composite actions under `.github/actions/`, referenced by path after the checkout.

  Still open: 4d and 4e have no checks to wire yet.

- [x] **4g — Enforce the commit convention.** **Done 2026-09-17, as a gate.** The `commits` gate in [check.py](src/llmctl/check.py) lints every subject in the pull request's range plus its title: the type must be one of the eight, and a scope must name a package the commit touched or an area outside `packages/`. `rm(core):` and `steering:` were both in recent history and would now fail.

  The 2026-08-19 decision was the opposite — a `commit-msg` hook, on the grounds that a PR-time check can only produce rewrite work. That is still true, and a hook is still welcome as a *second* layer (commitlint or conventional-pre-commit, installed out of band since APM cannot deploy git hooks). It is no longer the only option worth having: the type stopped sizing the version bump when versions became calendar-derived, so a bad type now costs a misfiled release-note heading rather than a silently wrong version, and a gate that catches it before merge is proportionate.

- [x] **4h — Decide how upstream content gets read before a pin moves.** **Done 2026-09-17.** The `meta-update-repo` skill makes the reading a required phase of every bump: `apm run update` prints the upstream's own diff for every pin it moved, filtered to the path this repo consumes, and [safety-review.md](.apm/skills/meta-update-repo/references/safety-review.md) says what to look for and what each finding means. A blocking finding stops the bump. The reference states the 2026-08-19 finding as its reason for being prose rather than a rule table. Original note: A pinned dependency is content an agent reads as instructions, and some of it ships scripts and hooks that run locally (`apm approve` gates *execution*, not content). The pin fixes **which** content arrives, not what it does, and a pin bump is the only moment where looking is cheap — the change is bounded and someone is already reviewing it.

  **A static rule table was prototyped and dropped 2026-08-19.** Matching strings — "ignore previous instructions", `~/.ssh`, `curl … | sh` — produced confident-looking findings on ordinary documentation (a vendor's own install one-liner, AWS's published placeholder key) while missing anything phrased differently. The two failure modes compound: the noise trains you to skim exactly the diff the check existed to make you read. If this returns it should be a model reading the diff of the materialised content, with this repo's own steering as context, reporting to a human — not a regex table.

  Deterministic subsets are still worth having, and APM may already cover them: `apm audit --file` scans an arbitrary path for hidden Unicode (bidi controls, tag characters — invisible to a reviewer, not to a tokenizer), and `apm audit --external skillspector` is a prompt-injection scanner, currently `[experimental]` behind `apm experimental enable external-scanners`, with an LLM mode that needs an API key and makes outbound calls. Re-evaluate both before writing anything local. Until then, reading the upstream diff is the reviewer's job.

- [ ] **4i — Hand upstream pin updates to Renovate when its APM manager ships.** Nothing tells you a pinned upstream moved. **Decided 2026-08-20: no custom code for this.** A `check-upstreams.py` was written and dropped unmerged; the two candidates are tracked here until one of them lands:

  - **Dependabot** — [dependabot-core#14350](https://github.com/dependabot/dependabot-core/issues/14350), open since 2026-03-03 with no maintainer response. Dependabot has no custom-manager escape hatch, so there is nothing to do here until GitHub builds the ecosystem.
  - **Renovate** — [renovate#44390](https://github.com/renovatebot/renovate/pull/44390) adds a first-class `apm` manager: it reads `dependencies.apm` / `devDependencies.apm`, handles `[host/]owner/repo[/subpath]#<ref>`, and runs `apm install` to sync the lockfile. **Open, not merged** as of 2026-08-13. Tracked upstream as [microsoft/apm#639](https://github.com/microsoft/apm/issues/639) (triaged, needs-design) and [renovate#42507](https://github.com/renovatebot/renovate/discussions/42507).

  **A second gap, found 2026-09-17, now handled in code.** `apm update` cannot move two of this repo's pins: it resolves a full-SHA pin only to the newest *annotated* semver tag, and `blader/humanizer` (5 tags, none annotated) and `rshade/agent-skills` (no tags) publish none. [update.py](src/llmctl/update.py) detects exactly that case — `git ls-remote --tags` dereferences an annotated tag into a `^{}` row, so "publishes no annotated semver tag" is a question git answers — and moves those pins to upstream HEAD itself. Any automation built on `apm update` alone still has the blind spot.

  **A third gap, found 2026-09-18 on APM 0.31.0, still open upstream.** `apm update` fails outright on a package that pins several subpaths of one repository at the same commit: `Failed to update apm.yml revision pins: Expected exactly one apm.yml entry for <sha>, found 3. No manifest changes were written.` That is `packages/design` (three `Design-Machines-Studio/depot` subpaths at one SHA); `packages/baseline` with two `mattpocock/skills` subpaths updates fine, so the boundary is somewhere above two. Nothing is written, so the failure is safe, and `update.py` reports it per package and exits non-zero rather than hand-editing around it. Worth filing upstream with the two-versus-three observation.

  **That gap closed itself on 2026-09-18.** The manager treats a SHA pin as a digest and recovers the version from a trailing tag comment — the GitHub Actions shape, `owner/repo#<sha> # v2.0.0` — which every entry here lacked. APM 0.31's `apm update` now writes exactly that annotation when it moves a pin, comments and all, so the convention arrives on its own as each pin is bumped. `blader/humanizer` publishes no tags and stays uncovered either way. Renovate's generic `customManagers` + `git-refs` would work today, but it tracks the repo's default-branch HEAD, so every unrelated commit in a monorepo upstream would open a PR against a skill that did not change.

---

## 5 — Eval Framework for Steering Effectiveness

**Goal:** Be able to answer "did that change actually make the steering better?" with evidence, and detect when a model upgrade makes existing steering redundant, harmful, or newly necessary.

**Priority: medium (high leverage, meaningful build cost — scope a minimal version first).**

### Background

Section 5 checks that content is *well-formed and deployed*; nothing checks that it is *effective*. Steering quality is currently judged by hand, so revisions are unfalsifiable and each new model release silently shifts the baseline (the [meta-update-models skill](.apm/skills/meta-update-models/SKILL.md) tracks model *identifiers*, not behaviour). A usable framework needs: a fixture set of representative tasks per package, a rubric or programmatic assertion per fixture, and A/B runs (steering on vs off, revision A vs B) across at least two model versions. Harness choice is open — a headless agent SDK loop, an off-the-shelf eval runner, or LLM-as-judge scoring — and should be decided before authoring fixtures.

Cost and non-determinism mean this is unlikely to belong on every PR; assume a manually triggered and/or scheduled job, reported as a tracked score over time rather than a pass/fail gate.

### Prior art — three upstream approaches, cheapest first

| Approach | Source | Shape | Cost / fidelity |
| --- | --- | --- | --- |
| **Per-skill eval fixtures** | [rshade/agent-skills](https://github.com/rshade/agent-skills) — e.g. [`skills/markdownlint/evals/`](https://github.com/rshade/agent-skills/tree/main/skills/markdownlint/evals) | One `evals.json` per skill: `{prompt, expected_output, files[], expectations[]}` with fixture files alongside; `expectations` are natural-language assertions for a judge | Cheapest. Fixtures live next to the skill, so they survive refactors. Good candidate for **5a**'s format |
| **Pressure tests + coverage matrix** | [antonbabenko/terraform-skill](https://github.com/antonbabenko/terraform-skill/tree/master/tests) | `baseline-scenarios.md` (expected *and* forbidden signals per scenario) plus `rationalization-table.md` mapping each hallucination surface → the scenario that exercises it → the guard line that must catch it, marked ✅/◐/❌ | Medium. The coverage matrix is the transferable idea: it makes "which steering line earns its place" auditable — directly serves **5c** |
| **Subagent behaviour drills** | [obra/superpowers](https://github.com/obra/superpowers) — `skills/*/test-pressure-*.md`, `tests/explicit-skill-requests/`, [superpowers-evals](https://github.com/prime-radiant-inc/superpowers-evals/) | Skill run against fresh subagents under adversarial prompts that tempt it to skip the process; also a token-usage analyzer | Highest fidelity, highest cost. Their `writing-skills` skill frames skill authoring as TDD: write the pressure scenario, watch the baseline fail, write the skill, watch it pass |

All three are "prompt + assertions, judged by a model" — none requires a bespoke harness, which lowers the bar for **5a** considerably.

### Tasks

- [ ] **5a — Choose the eval harness and scoring model** (runner, judge vs deterministic assertions, how a run is pinned to a model version and a repo revision). Decide before writing fixtures — it dictates their format.
- [ ] **5b — Build evals for this repo's own content.** Fixtures per package (`baseline`, `ops`, `product`, and the root `.apm/` procedures) covering the behaviours the steering is meant to produce, run with and without the steering loaded, scored and recorded so revisions can be compared. Start with one package end-to-end before fanning out.
- [ ] **5c — Track scores across model versions.** Re-run the suite on new model releases to see where steering stops earning its place (or where a regression needs new steering), and feed the outcome back into `meta-update-models` and the [reflect prompt](packages/baseline/.apm/prompts/reflect.prompt.md).
- [ ] **5d — Fold the skill-as-TDD method into the `meta-*` skills** once 5a exists. [obra/superpowers `writing-skills`](https://github.com/obra/superpowers/blob/main/skills/writing-skills/SKILL.md) treats authoring as test-driven: write the pressure scenario, run it against a *fresh subagent* to establish the failing baseline, write the skill, re-run until the agent complies, then refactor to close the loopholes it rationalised through — with [`testing-skills-with-subagents.md`](https://github.com/obra/superpowers/blob/main/skills/writing-skills/testing-skills-with-subagents.md) as the procedure. That closes the gap in [meta-steering](packages/baseline/.apm/skills/meta-steering/SKILL.md), which specifies structure and description rules but has no way to show a skill actually changes behaviour. Extend across its per-type references (agents, prompts, instructions) and to `meta-harness` (hooks) where the same subagent-pressure method applies. Blocked on 5a: the method needs a harness to run the drills. Record as `metadata.provenance.adaptedFrom`.
- [ ] **5e — Extend evals to third-party / integrated content.** Cover the pinned APM dependencies in `packages/*/apm.yml` and `adaptedFrom` content so an upstream update can be scored before adoption, and so vendored adaptations can be compared against their upstream original. Gated on 5a–5b proving out.

---

## 6 — Marketplace Repository: Licensing, Versioning, Release Automation

**Goal:** Make [`.llmctl-marketplace`](https://github.com/siegenthalerroger/.llmctl-marketplace) publishable without hand-maintenance — correct attribution in every bundle, versions that move on their own, and a release path that does not depend on someone remembering to run a script.

**Priority: low** No public consumers

### Background — what exists now

Marketplace manifests and packed bundles live in a **separate repository**, generated by [pack_marketplace.py](src/llmctl/pack_marketplace.py) (wired as `apm run pack-marketplace`). The split exists because a plugin host clones the marketplace repo and reads each `packages[].source` path *as committed* — it never runs `apm install` — so packages carrying APM dependencies must ship as bundles with those skills vendored in. `apm pack` also rejects `..` in a marketplace output path, so the manifest has to be generated in the marketplace repo itself. Licence files are added by the script, so every bundle carries the attestations it owes.

**As of 2026-09-17 the marketplace repositories hold nothing authored.** Their `README.md`, `LICENSE`, `.gitignore` and `apm.yml` are written from `*.marketplace.*` sources in this workspace, anything else in the tree is deleted on the way past, and their CI is gone. Versions are calendar-derived and recorded as tags, so a release commits nothing here either.

**Since 2026-09-18 the pack gate also audits what it packs.** Each scratch export runs `apm audit --ci` after its install, so the vendored upstream content is scanned for critical hidden Unicode and lockfile hash drift before it becomes a bundle, and `apm pack --json` reports version alignment as data rather than as a line of stdout to match.

### Tasks

- [ ] **6a — Go public and verify end to end.** The marketplace repo is private. That does **not** block Cowork — a private marketplace installs there against an authenticated remote — so going public is about reach, not capability. Use an SSH source for a private one: background marketplace auto-updates run with git credential helpers disabled, so an HTTPS private remote fails the pull and falls back to a re-clone. **Unblocked as of 2026-08-02** — the split licensing is settled and every bundle carries its `LICENSE`, `LICENSES/` and generated `THIRD-PARTY-NOTICES.md`. Flipping the repo public is a GitHub setting, so it needs doing by hand. Then install a plugin end to end from Cowork and record what actually loaded (skills, commands, and whether 0.26's packed `agents/`, `instructions/`, and `.mcp.json` are honoured — the previous finding that they do not travel predates 0.26).

- [ ] **6b — Get the Codex marketplace entries to carry their own metadata.** Bundles now ship `.codex-plugin/plugin.json` (Codex does not read the Claude manifest), which is what makes them installable; pack_marketplace.py writes it, and nothing verifies it afterwards. The catalogue around them is still thin: APM writes `version`, `description` and `category` into `.claude-plugin/marketplace.json` but emits only `name`, `source`, `policy` and `category` per entry in `.agents/plugins/marketplace.json`, even though all of it is declared in the marketplace `apm.yml`. Nothing breaks — [openai/codex#28789](https://github.com/openai/codex/pull/28789) makes the on-disk `plugin.json` win over marketplace metadata — but browsing the marketplace shows no version and no description. It is APM's emitter to fix, so: report it upstream rather than post-processing a generated file. While there, `interface.capabilities` is specified only as a free-form "capability list from implementation"; `write_codex_manifest` reports what each bundle carries, which is a reading, not a documented vocabulary. Confirm it against a real `codex plugin validate` — the Codex CLI is not in CI, so nothing checks the Codex manifest the way `claude plugin validate` checks the Claude one.

- [ ] **6c — Get `apm pack` to emit the Codex plugin manifest, and delete `write_codex_manifest`.** `apm pack` writes `.claude-plugin/plugin.json` and nothing else — its `--target` help names claude and copilot — so [pack_marketplace.py](src/llmctl/pack_marketplace.py) hand-writes the `.codex-plugin/plugin.json` beside it from the manifest APM just produced. That is a gap in APM, not a decision here: the same `apm.yml` supplies every field except `interface`, and APM already emits the Codex *marketplace* manifest, so it knows the host exists. **No upstream issue covers this** — searched microsoft/apm on 2026-08-20 for `codex`, `plugin.json`, `codex-plugin` and `marketplace.json`; the nearest is [microsoft/apm#2524](https://github.com/microsoft/apm/issues/2524) (pack drops description/version for *remote* packages when a metadata fetch fails), which is a different cause. So the first step is filing one, together with **6b** — both are the same omission seen from the bundle side and the catalogue side. Until then `write_codex_manifest` is ours to keep correct against a spec we do not own; delete it in the change that adopts APM's.
