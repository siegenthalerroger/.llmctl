# Secret Safety
.env

# Dependency copies
apm_modules/

# Local / machine-generated settings (APM-deployed wiring lands in settings.local.json;
# .claude/settings.json stays free for intentional, shared project settings)
.claude/*.local.json
